# Obsidian

Open the installed library folder as an Obsidian vault. Enable wikilinks. Optional: install Dataview. Keep `_INBOX_USER/` as the safe write lane.

## Safety

- Do not expose Syncthing GUI publicly.
- Do not sync secrets or `.env` files.
- Write human notes into `_INBOX_USER/`.
- Let agents promote reviewed material into canon.
